const { BSTNode, BinarySearchTree } = require('./BinarySearchTree')

describe('Tree classes test Suite', () => {
  test('should retun a node', () => {
    const bstNode = new BSTNode('data');
    console.log(bstNode)
  })
})
